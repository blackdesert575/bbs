[project]
name = "main"
version = "0.1.0"
description = "make a bbs with Django"
authors = [{ name = "Hong", email = "yunghunghuang984@gmail.com" }]
requires-python = "==3.13.13"
readme = "README.md"
license = "MIT"
dependencies = [
    "django-environ==0.10.0",
    "django==5.2.14",
    "psycopg[binary]>=3.3.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
